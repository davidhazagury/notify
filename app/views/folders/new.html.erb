<%= simple_form_for [current_user, @folder] do |f| %>
<%= f.label :folder_name, label: 'Enter the name of the new folder', required: true %>
<%= f.text_field :folder_name %>
<%= f.submit %>
<% end %>
